- id: generate_response
  name: Generate Response
  type: llm
  prompt: "What are the key points in: {{ document_content }}"
  model: deepseek-r1  # Can also use 'qwen' as an alternative
  stream: false
  outputs: [response]